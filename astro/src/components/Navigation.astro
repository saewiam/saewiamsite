---
import { getNavigationLinks } from '../repositories/NavigationLink.ts'

const links = await getNavigationLinks()
---

<nav>
    <ul>
        {
            links.map((link: any) => (
                <li>
                    <a href={link.url}>{link.title}</a>
                    <ul>
                        {link.sublinks.map((sublink: any) => (
                            <li>
                                <a href={sublink.url}>{sublink.title}</a>
                            </li>
                        ))}
                    </ul>
                </li>
            ))
        }
    </ul>
</nav>
